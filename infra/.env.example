# Copy to `infra/.env` (or repo root `.env`) for local compose / tooling.
# Phase 0.3 expands this file with every required variable and documentation.

# DATABASE_URL=postgresql://tracehub:tracehub@localhost:5437/tracehub
